#include <stdio.h>

int main() {
    int n;
    printf("Введите размер матрицы: ");
    scanf("%d", &n);

    int A[n][n], X[n];

    printf("Введите элементы матрицы:\n");
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            scanf("%d", &A[i][j]);
        }
    }

    // Расчет элементов массива X
   for (int i = 0; i < n; i++) {
        int a = 1; // Флаг, указывающий, что все элементы строки удовлетворяют условию
        for (int j = 0; j < n; j++) {
            if (A[i][j] > A[j][i]) {
                a = 0; // Если условие не выполняется, флаг сбрасывается
            }
        }
        X[i] = a;
    }

    // Расчет суммы элементов массива X
    int sum = 0;
    for (int i = 0; i < n; i++) {
        sum += X[i];
    }

    printf("Сумма элементов массива X: %d\n", sum);

    return 0;
}
#include <stdio.h>

int main() {
    int n;
    printf("Введите размер матрицы: ");
    scanf("%d", &n);

    // Объявление двумерного массива A и одномерного массива X
    int A[n][n], X[n];

    // Ввод элементов матрицы
    printf("Введите элементы матрицы:n");
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            scanf("%d", &A[i][j]);
        }
    }

    // Расчет элементов массива X
    for (int i = 0; i < n; i++) {
        int a = 1; // Флаг, указывающий, что все элементы строки удовлетворяют условию
        for (int j = 0; j < n; j++) {
            // Проверяем, больше ли элемент строки i, чем соответствующий элемент в столбце j
            if (A[i][j] > A[j][i]) {
                a = 0; // Если условие не выполняется, флаг сбрасывается
                break; // Выход из внутреннего цикла, так как условие не выполнено
            }
        }
        X[i] = a; // Записываем значение флага в массив X
    }

    // Расчет суммы элементов массива X
    int sum = 0;
    for (int i = 0; i < n; i++) {
        sum += X[i];
    }

    // Вывод элементов массива X
    printf("Элементы массива X:n");
    for (int i = 0; i < n; i++) {
        printf("%d ", X[i]);
    }
    printf("n");

    // Вывод суммы элементов массива X
    printf("Сумма элементов массива X: %dn", sum);

    // Вывод самой матрицы
    printf("Элементы матрицы A:n");
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            printf("%d ", A[i][j]);
        }
        printf("n");
    }

    return 0;
}#include <stdio.h>

int main() {
    int n;
    printf("Введите размер матрицы: ");
    scanf("%d", &n);

    // Объявление двумерного массива A и одномерного массива X
    int A[n][n], X[n];

    // Ввод элементов матрицы
    printf("Введите элементы матрицы:n");
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            scanf("%d", &A[i][j]);
        }
    }

    // Расчет элементов массива X
    for (int i = 0; i < n; i++) {
        int a = 1; // Флаг, указывающий, что все элементы строки удовлетворяют условию
        for (int j = 0; j < n; j++) {
            // Проверяем, больше ли элемент строки i, чем соответствующий элемент в столбце j
            if (A[i][j] > A[j][i]) {
                a = 0; // Если условие не выполняется, флаг сбрасывается
                break; // Выход из внутреннего цикла, так как условие не выполнено
            }
        }
        X[i] = a; // Записываем значение флага в массив X
    }

    // Расчет суммы элементов массива X
    int sum = 0;
    for (int i = 0; i < n; i++) {
        sum += X[i];
    }

    // Вывод элементов массива X
    printf("Элементы массива X:n");
    for (int i = 0; i < n; i++) {
        printf("%d ", X[i]);
    }
    printf("n");

    // Вывод суммы элементов массива X
    printf("Сумма элементов массива X: %dn", sum);

    // Вывод самой матрицы
    printf("Элементы матрицы A:n");
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            printf("%d ", A[i][j]);
        }
        printf("n");
    }

    return 0;
}
